pipeline {
    agent any

    environment {
       FAST_API_SERVICE_CONTAINER = "fast-api-container"
       FAST_API_SERVICE = "fast-api"
    }

    stages {

        stage('Checkout') {
            steps {
                git 'https://github.com/raison-collab/TutorialServiceFastAPI.git'
            }
        }

//        stage('Test') {
//            steps {
//                script {
//                    docker.image(FAST_API_SERVICE).inside {
//                        sh 'pytest'
//                    }
//                }
//            }
//        }

        stage('Deploy') {
           steps {
               script {


                   // Остановка и удаление старого контейнера
                   sh """
                   docker-compose down
                   docker-compose up -d --build ${FAST_API_SERVICE}
                   """
               }
           }
        }
    }

    post {
       always {
           cleanWs()
       }
    }
}